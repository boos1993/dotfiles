[user]
	name = GIT_GLOBAL_USER_NAME
	email = GIT_GLOBAL_USER_EMAIL
[core]
	editor = vim
	whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = ~/.gitignore

[alias]
    l = log --oneline
	lg = log --oneline --graph --decorate

[pull]
	rebase = true