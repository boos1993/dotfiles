set number
set relativenumber
set nowrap
set autochdir

cnoremap sudow w !sudo tee % >/dev/null

colorscheme evening

" copy and paste
vmap <C-c> "+yi
vmap <C-x> "+c
vmap <C-v> c<ESC>"+p
imap <C-v> <ESC>"+pa
